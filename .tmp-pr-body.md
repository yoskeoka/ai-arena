## Plan / Issues

- **Plan**: `docs/exec-plan/done/0087-product-auth-and-gated-signup-01-github-login-and-account-linking-foundation.md`
- **Issues**: `docs/issues/0030-operator-ui-playwright-auth-coverage-gap.md` remains open as follow-up work for auth-inclusive Playwright verification

## Type of Change

- [ ] Project Plan update
- [ ] Execution Plan (new/updated plan)
- [x] Feature implementation
- [ ] Bug fix
- [ ] Refactor
- [ ] Documentation only
- [ ] Chore (CI, tooling, deps)

## Change Summary

- add GitHub auth bootstrap route cleanup by moving browser session status to `/auth/session`
- add local bootstrap entrypoints for manual backend/frontend startup and fresh local invite URL generation
- restore Postgres schema SSoT parity by adding auth tables to desired schema files used by `postgres-schema-apply`
- document local auth verification flow and log the remaining Playwright auth coverage gap as `docs/issues/0030-operator-ui-playwright-auth-coverage-gap.md`

## Human Instructions / Intent

Human instruction: `/execute-task docs/exec-plan/todo/0087-product-auth-and-gated-signup-01-github-login-and-account-linking-foundation.md`
### Additional Context from Instructing Human

- Human required OAuth callback paths to include the provider, so the first landing uses `/auth/github/callback`.
- Human chose backend-side OAuth callback handling over frontend callback handling because current `pages.dev` / `onrender.com` split-origin cookie behavior is cleaner that way.
- Human explicitly required `ARENA_<PROVIDER>_OAUTH_CLIENT_ID` / `ARENA_<PROVIDER>_OAUTH_CLIENT_SECRET` naming and required the agent to avoid reading `.env`; local secret injection had to go through `direnv exec` or equivalent human-managed shell setup.
- Human asked to extend the auth task slightly so local developer bootstrap works through repo-owned `make` targets before manual GitHub login verification.
- Human verified locally that GitHub login succeeds, the http-only cookie is issued, and logout clears the cookie.
- Human asked to move the session status route out of `/api/v1/*` and into the auth namespace, which is implemented here as `GET /auth/session`.

## Verification

- [x] Tests pass (command: `go test ./...`)
- [x] Lint passes (command: `./tools/workflow-lint.sh --mode=pre-push` and `pnpm run textlint:file -- README.md DEVELOPMENT.md docs/development/operator-ui-local-verification.md docs/development/platform-service-online-deploy.md docs/specs/platform-product-auth.md docs/specs/platform-service-operator-api.md docs/issues/0030-operator-ui-playwright-auth-coverage-gap.md`)
- [x] Manual verification (describe below)

Manual verification:
- Human verified local GitHub login using the repo-owned local bootstrap flow.
- Human verified the backend issues an http-only session cookie and that logout clears it.
- Fresh local invite URLs were verified through `make local-invite-url`.
- Frontend build verification: `cd operator-ui && XDG_DATA_HOME=/tmp/.xdg-data-ai-arena PNPM_HOME=/tmp/.pnpm-home-ai-arena pnpm_config_store_dir=/tmp/pnpm-store-ai-arena pnpm run build`

## Deterministic Golden Updates

- [ ] This PR updates a deterministic regression golden
- Allowed update reason: `N/A`
- Spec/plan reference that records the same reason: `N/A`

## Checklist

- [x] Branch created from latest `origin/main`
- [x] `docs/specs/` updated (Spec-Code Parity) — _if code changed_
- [x] Plan moved from `todo/` to `done/` — _if executing a plan_
- [x] Resolved linked local issues from the plan's `Addresses:` line were moved to `docs/issues/done/`, or this PR explains why they remain open
- [x] Workflow-linter warnings reviewed; all `fixable` warnings were resolved or explicitly justified in this PR
- [x] New issues logged in `docs/issues/` — _if discovered during work_
- [x] No unresolved blockers remain

## Dependencies

N/A

## Reviewer Notes

- The release workflows already use `make postgres-migrate-apply`, so the Postgres schema SSoT fix mainly affects local/bootstrap lanes and future migration generation rather than the current staging/production apply path.
- `docs/issues/0030-operator-ui-playwright-auth-coverage-gap.md` tracks the remaining auth-inclusive Playwright gap and the now-misleading `Skip to operator route` login-page affordance.

## Links

- GitHub OAuth app creation: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/creating-an-oauth-app
- GitHub OAuth web application flow: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps

## Breaking Changes

N/A

## Screenshots / Logs

- Local manual verification was performed directly in the browser by the human; no screenshot artifact was captured in this branch.
