Place execution plans here.
Format: `description.md` or `description-step.md` when ordering matters (e.g. `platform-phase2-01-foundation.md`).
When a plan is completed and the PR is ready, move the file to `../done/`.
