Place execution plans here.
Format: `<sequence>-description.md` or `<sequence>-description-step.md` when ordering matters (e.g. `0013-platform-phase2-01-foundation.md`).
Use the next available sequence number so active plan creation order stays visible without checking Git history.
When a plan is completed and the PR is ready, move the file to `../done/`.
